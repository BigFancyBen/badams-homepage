# FloatWise map — real weather station locations (Synoptic Data mesonet API).
# Free token for non-commercial use: https://synopticdata.com (Profile → Data Credentials).
# Read server-side only (never exposed to the browser).
SYNOPTIC_TOKEN=

# Commander multiplayer (Ably realtime).
ABLY_API_KEY=
